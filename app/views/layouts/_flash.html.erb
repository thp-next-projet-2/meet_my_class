<% flash.each do |key, value| %>
  <div class="<%= flash_class(key) %> alert-dismissable fade show" role="alert">
    <%= value %>
    <button type="button" class="close" data-dismiss="alert" aria-label="Close">
      <span aria-hidden="true">&times;</span>
    </button>
  </div>
<% end %>
