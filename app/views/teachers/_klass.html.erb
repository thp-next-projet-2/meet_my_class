<div class="card">
    <div class="card-body">
      <h5 class="card-title">
          <%= link_to klass.title, teachers_klass_path(klass.id) %>
      </h5>
      <p class="card-text"><%= klass.description %></p>
    </div>
  </div>
