<!-- <h3>Participants :</h3>
Nombre : <%= @klass.students.count %>
<ul>
  <% @klass.students.order(:email).each do |student| %>
  <li>
    <%= student.email %> <%= student.klasses.all.pluck(:id) %>
  </li>
  <% end %>
</ul>
<h3>Inviter :</h3>
<ul>
  <% @not_attending.order(:email).each do |student| %>
  <li>
    <%= student.email %> <%= student.klasses.all.pluck(:id) %>
  </li>
  <% end %>
</ul>

<h3>Inviter nouveau :</h3>
<%# simple_form_for @student_new, url: student_registration_path do |f| %>
<%= simple_form_for @student_new, controller: 'students', action: 'create' do |f| %>
<%# simple_form_for new_student_path do |f| %>
<%= f.input_field :email,
                  required: true,
                  autofocus: false,
                  placeholder: "email" %>
<%= f.hidden_field :klass_id, value: params["id"] %>
<%= f.hidden_field :password, value: @generated_password %>
<%= f.submit "Inviter" %>
<% end %> -->



<div class="card col-12">
  <h2> Inviter un élève</h2>
  <%= form_tag(klass_attendances_path(@klass), authenticity_token: true,  method: 'POST', multipart: true) do%>
    <div class="row form-group card-body text-primary">
      <div class="col-8 text-right">
        <%= email_field_tag 'attendance[email]', '', class: ' form-control ', placeholder: 'Email'%>
      </div>
      <div class="col-4 text-right">
        <%= submit_tag "Inviter", class: " btn btn-success" %>
      </div>
    </div>
  <%end%>
</div>

<div class="row">
		<div class="col-md-6 card p-3">
			Déjà inviter
			<ul class="list-group">
				<% @invitations.each do |invitation| %>
  				<li class="list-group-item">
  					<%=invitation.student.email%>
  					<%= link_to '<i class="far fa-times-circle"></i>'.html_safe, klass_attendance_path(@klass, invitation), method:'DELETE'%>
  				</li>
  			<%end%>
			</ul>
		</div>
		<div class="col-md-6 card p-3">
			En attente d'invitation
			<ul class="list-group">
				<% @not_attending.order(:email).each do |student| %>
  				<li class="list-group-item">
  					<%= student.email%>
  						<%= link_to '<i class="far fa-times-circle"></i>'.html_safe, klass_attendance_path(@klass, student.attendances), method:'DELETE'%>
  				</li>
  			<%end%>
			</ul>
		</div>
	</div>
