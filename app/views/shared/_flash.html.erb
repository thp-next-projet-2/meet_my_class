<% flash.each do |type, msg| %>
  <div id="flash">
  <div class="alert <%= bootstrap_flash_messages(type) %> alert-dismissable fade show" role="alert">
    <%= msg %>
  <button type="button" class="close" data-dismiss="alert" aria-label="Close">
    <span aria-hidden="true">×</span>
  </button>
  </div>
</div>
<% end %>